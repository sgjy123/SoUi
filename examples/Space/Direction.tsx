import React from 'react';
import { Space, Button } from '../../src';

export default () => (
  <Space direction="vertical">
    <Button type="primary">垂直排列</Button>
    <Button>垂直排列</Button>
  </Space>
);
