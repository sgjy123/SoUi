实现 TreeSelect 树形下拉组件（参考 Ant Design API）。包含：
- `src/components/TreeSelect/index.tsx` 与 `style.less`：支持单选、多选、勾选（含父子联动/严格模式）、搜索、异步加载、尺寸、状态、主题变量注入等。
- `examples/TreeSelect/`：Basic、Multiple、Checkable、Searchable、Theme 五个示例。
- `src/docs/components/tree-select.md`：完整组件文档（API、主题定制、无障碍、FAQ 等）。
- 修复 `ConfigProvider` 中 TreeSelect 文本颜色回退值引用不存在属性的类型错误。
- 已通过 `npx tsc --noEmit` 类型检查与 `npm run build` 构建验证。

后续补充 TreeSelect 示例：新增 AsyncLoad、ControlledExpanded、CustomFieldNames、DisabledNodes、LargeData 五个示例；在 `src/demo/config.ts` 完成注册；更新 `src/docs/components/tree-select.md` 增加对应章节。再次通过 `npm run build` 构建验证。
